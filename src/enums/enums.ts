export enum UserType {
  USER = 'user',
  ADMIN = 'admin',
  BANNED = 'banned',
  SUPERADMIN = 'superadmin',
}

export enum OrderStatus {
  PENDING = 'pending',
  PROCESSING = 'processing',
  COMPLETED = 'completed',
  CANCELLED = 'cancelled',
}
